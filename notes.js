//arrays are lists [0,1,2]
//arrays have methods (cut in half, copy, filter, map)

//objects are property-value pairs of a thing (cars, students, etc.) --> {name: 'Thai', email: 'thai@gmail.com', age: 33, sex: 'male', classes: ['History','Math'], property: value}

//functions 

/* 
const someFunction = () => {
    //perform actions

    //can also return
    return //this ends the function, (returns undefined)

    return true //returning the boolean true

    //if no return keyord is found, returns undefined
}

someFunction() //invoke the function

let x = someFunction() //invokes the function, but also saves whatever someFunction returns into the value x
*/